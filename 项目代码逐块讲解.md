# 项目代码逐块讲解

> 把 `agent-from-zero` 目录里每个文件、每段代码讲明白。
> 建议阅读顺序：先看第 1 节全景图，再按 config → 01 → 02 的顺序读。

---

## 1. 目录全景：每个文件是干什么的

```
agent-from-zero/
│
├── config.py              ← 你所有脚本的「后勤部」：管服务商配置和 Key
├── 01_test_connection.py  ← 最小调用示例：一问一答，看清协议
├── 02_agent_loop.py       ← 阶段 1 练习：手写 Agent 循环（有 TODO 等你写）
│
├── .env                   ← 你的真实 API Key（不进 Git）
├── .env.example           ← Key 的模板（进 Git，给别人看格式）
├── .gitignore             ← 告诉 git 哪些文件不进仓库
│
├── pyproject.toml         ← 项目说明书：名字、Python 版本、依赖清单
├── uv.lock                ← 依赖的精确版本快照（自动生成，别手改）
├── .python-version        ← 记录本项目用的 Python 版本（自动生成）
├── .venv/                 ← 虚拟环境，隔离的 Python 解释器和依赖包
│
└── README.md              ← 项目门面：怎么用、目标清单
```

**调用关系一条线**：`01` 或 `02` 脚本 `import config` → `config.py` 执行 `load_dotenv()` 读 `.env` → 拿到 Key → 脚本用 Key 创建客户端 → 发请求给百炼服务器。

---

## 2. 项目管理三件套：pyproject.toml / uv.lock / .python-version

这三个是 `uv init` 和 `uv add` 自动生成维护的，一般不用手改，但要看得懂。

### pyproject.toml —— 项目的"身份证 + 购物清单"

```toml
[project]
name = "agent-from-zero"        # 项目名
requires-python = ">=3.12"      # 最低 Python 版本要求
dependencies = [                # 依赖清单：声明"我需要这两个库"
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
]
```

- `dependencies` 是你以后最常碰的部分：想加新库时跑 `uv add 库名`，它会自动写进这里并安装。
- 别人拿到你的项目，跑一句 `uv sync` 就能照这份清单还原出完全一样的环境——这就是"依赖声明"的价值。

### uv.lock —— 精确到每个小版本的快照

`pyproject.toml` 写的是"范围"（`>=3.3.1`），而 `uv.lock` 记录的是你**实际装到的精确版本**以及所有间接依赖。作用：保证你换台电脑、或三个月后再装，得到的是和今天一模一样的依赖组合。提交进 Git。

### .python-version / .venv

- `.python-version`：记录本项目用哪个 Python（uv 会自动按它找/装）。
- `.venv/`：虚拟环境本体。你装的 openai 库其实在这个文件夹里，不污染系统 Python。**不要动它、不要提交它**，删了也没关系（`uv sync` 会重建）。

---

## 3. 密钥三件套：.env / .env.example / .gitignore

前面已经详细聊过机制，这里只说分工：

| 文件 | 内容 | 进不进 Git |
|------|------|-----------|
| `.env` | 你的真实 Key | **不进**（被 .gitignore 排除） |
| `.env.example` | 占位符模板 `sk-your-api-key-here` | 进（别人照着填自己的） |
| `.gitignore` | 排除规则，第 13 行的 `.env` 就是那条规则 | 进 |

`.env` 的格式是"每行一个 `变量名=值`"，本项目里就一行：

```
DASHSCOPE_API_KEY=sk-你的真实key
```

`config.py` 启动时会把这个值加载成环境变量，代码里只认变量名，永远碰不到 Key 本身。

---

## 4. config.py —— 后勤部

全文只做一件事：**你要用哪个服务商，我就把它的配置和 Key 给你**。逐块看：

### 4.1 头部：加载 .env

```python
import os
from dotenv import load_dotenv

load_dotenv()   # 读 .env 文件，把里面的键值对塞进环境变量
```

- `load_dotenv()` 是 python-dotenv 库的核心，一行顶替手动 `set` 环境变量。它有个重要特性：**已经存在的环境变量不会被 .env 覆盖**——所以"系统环境变量"优先级高于".env 文件"。
- 注意：这行代码必须发生在任何 `os.getenv` 之前，所以放在模块顶层（import config 时就会执行）。

### 4.2 PROVIDERS 字典 —— 服务商通讯录

```python
PROVIDERS = {
    "qwen": {
        "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1",
        "api_key_env": "DASHSCOPE_API_KEY",
        "default_model": "qwen-plus",
    },
}
```

每个服务商三个字段：
- `base_url`：API 的地址。**关键点**：百炼这个地址末尾的 `compatible-mode` 表示"OpenAI 兼容模式"——即请求格式、字段名和 OpenAI 官方完全一致。这就是为什么一个 `openai` 库能同时打天下所有国产模型：大家争相兼容 OpenAI 的协议事实标准。
- `api_key_env`：Key 存在哪个环境变量名下（只是"变量名的名字"，不是 Key 本身）。
- `default_model`：默认用哪个模型。`qwen-plus` 是能力/价格的平衡档；以后想省钱换 `qwen-turbo`，想拉满换 `qwen-max`，只改这一处。

下面注释掉的 deepseek / zhipu / kimi 块是给你以后准备的，取消注释 + .env 里填 Key 即可启用。

### 4.3 get_provider() —— 取货窗口

```python
def get_provider(name: str = "qwen"):
    provider = PROVIDERS[name]
    api_key = os.getenv(env_name, "")      # 从环境变量取 Key，取不到给空串

    if not api_key or api_key.startswith("sk-your"):
        raise RuntimeError("...还没配置，请打开 .env ...")   # 主动报错+指引
    return provider, api_key
```

两个设计值得学：
1. **防御式报错**：Key 没配时，不是让程序跑到一半抛出难懂的认证错误，而是在门口就拦下，用中文告诉你下一步干什么。写给别人用的代码都该这样。
2. `startswith("sk-your")` 连"你忘了替换占位符"这种情况都预判了。

---

## 5. 01_test_connection.py —— 最小完整调用

47 行，一次"问一句、答一句"的完整流程。逐块：

### 5.1 创建客户端

```python
client = OpenAI(base_url=provider["base_url"], api_key=api_key)
```

`OpenAI()` 是 SDK 的客户端对象，之后所有请求都通过它发。两个参数：指向哪（base_url）、凭什么（api_key）。**这个对象创建一次就复用，不要每次请求都新建**（内部有连接池）。

### 5.2 发起请求

```python
response = client.chat.completions.create(
    model=provider["default_model"],
    messages=[
        {"role": "system", "content": "你是一个简洁的助手，用一句话回答。"},
        {"role": "user", "content": "什么是 AI Agent？"},
    ],
    temperature=0.7,
)
```

这是整个阶段你最需要刻进肌肉记忆的一个调用：
- `model`：用哪个模型；
- `messages`：**完整的对话历史**（这里只有一问，多轮时就是长长的列表）。模型没有记忆，它的"记忆"就是这个数组；
- `temperature=0.7`：采样随机性，0~2，越高越发散。做 Agent 的工具决策链路时建议调低到 0~0.3。

### 5.3 拆解返回值

```python
response.choices[0].message.content   # 模型的回复文本
response.usage.prompt_tokens          # 输入消耗的 token
response.usage.completion_tokens      # 输出消耗的 token
```

返回结构树状展开是：

```
response
├── choices（为什么是列表？因为可以一次请求要多个候选回复，默认 1 个）
│   └── [0].message
│       ├── role: "assistant"
│       └── content: "回复文本"        ← 阶段 1 之后，这里还会出现 tool_calls 字段
└── usage
    ├── prompt_tokens / completion_tokens / total_tokens
```

`usage` 不是摆设——**按 token 计费的时代，盯用量就是盯钱**，面试聊成本优化时张口就来的数字全靠平时积累。

### 5.4 `if __name__ == "__main__":`

Python 的惯用写法：只有"直接运行这个文件"时才执行 `main()`；如果这个文件被别的文件 import，就不会执行。所有脚本都这么写，安全又规范。

---

## 6. 02_agent_loop.py —— 练习场

结构分三块：**已写好的工具定义、已写好的工具执行、留给你写的循环**。前两块逐块讲，第三块只讲要求。

### 6.1 顶部初始化（19–21 行）

```python
provider, api_key = get_provider("qwen")
client = OpenAI(base_url=provider["base_url"], api_key=api_key)
MODEL = provider["default_model"]
```

和 01 一样，只是放在了模块顶层（import 即执行）。

### 6.2 TOOLS —— 用 JSON Schema 给模型写"工具说明书"

以 `calculate` 为例：

```python
{
    "type": "function",                 # 工具类型（目前就只有 function 这种）
    "function": {
        "name": "calculate",            # 函数名，模型调用时按这个名字点名
        "description": "计算一个数学表达式的结果，例如 '3 * (4 + 5)'",
        "parameters": {                 # 参数说明，JSON Schema 格式
            "type": "object",
            "properties": {
                "expression": {"type": "string", "description": "数学表达式"}
            },
            "required": ["expression"], # 必填参数
        },
    },
}
```

三个认知点：
1. **这份说明书是模型做决策的唯一依据**。模型不会读你的 Python 代码，它只看 name、description 和 parameters。description 写得含糊，模型就会乱调或传错参数——所以写工具描述就是写提示词。
2. `get_current_time` 的 `properties` 是空的 `{}`——无参数工具也要给个空对象，格式不能省。
3. 模型看到这份说明书后，**只负责决定"调哪个、传什么"，不负责执行**。

### 6.3 execute_tool() —— 真正干活的手脚

```python
def execute_tool(name: str, arguments: dict) -> str:
    if name == "get_current_time":
        return datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    if name == "calculate":
        try:
            return str(eval(arguments["expression"]))
        except Exception as e:
            return f"计算失败：{e}"
    return f"未知工具：{name}"
```

三个设计点：
1. **统一入口**：不管模型调哪个工具，都走这一个函数分发（if/elif 路由）。以后加工具 = 这里加一个分支 + TOOLS 里加一段说明书。
2. **错误是返回值而不是崩溃**：`except` 住异常后把错误信息作为字符串返回——这条信息会回传给模型，模型看到"计算失败：xxx"有机会自己修正参数重试。这是 Agent"自愈"的机制源头。
3. `eval` 的安全警告是认真的：它会执行任意 Python 代码，生产环境要换成 `asteval` 之类的受限求值器。面试主动提这一点是加分项。

### 6.4 run_agent() —— 你的练习区（不剧透）

已写好的部分：初始 messages 列表、`max_steps` 步数上限、每轮的分隔打印。三个 TODO 要做的事在代码注释里写得很详细，对应的伪代码在《阶段1讲解》文档第 3 节。验收标准：

- `uv run 02_agent_loop.py` 后，"现在几点了？"能走通"模型调 get_current_time → 拿到时间 → 回答"全过程；
- 解开第二个 `run_agent` 的注释，"27 乘以 43"能走通 calculate 路径；
- 每一步都有轨迹打印。

---

## 7. README.md

项目的门面，包含快速开始命令和目标清单。以后你把项目放上 GitHub，面试官第一眼看到的就是它——**README 写得好 = 这个项目看起来专业**。目前的版本够用，等你完成阶段 1 目标后，建议把清单勾选状态更新，再补一段"运行效果示例"（贴一段轨迹输出截图或文本）。

---

## 8. 运行命令速查

```bash
cd D:\AgentLearning\agent-from-zero

uv sync                      # 还原依赖（换电脑/克隆仓库后第一件事）
uv add 库名                  # 加新依赖（会自动写进 pyproject.toml）
uv run 01_test_connection.py # 跑脚本（自动用 .venv 里的环境）
uv run 02_agent_loop.py      # 跑练习
```

`uv run` 的好处：不用手动激活虚拟环境，它自动用项目的 `.venv`。
